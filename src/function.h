#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <string.h>

bool wildcard(char);
bool checkValid(char*);
int sha(char*);

/*RESERVED FUNCTIONS*/

bool reservedJS(char*);
bool reservedC(char*);
bool reservedJAVA(char*);